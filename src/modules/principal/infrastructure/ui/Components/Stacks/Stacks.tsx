//
// React dependencies
//
import React from 'react';

interface StacksProps {
}

const Stacks: React.FC<StacksProps> = () => {
    return (
        <section className={'w-full flex flex-col md:flex-row lg:flex-row'}></section>
    )
};

export default Stacks;