//
// React dependencies
//
import React from 'react';

// Use type Record<never, never> instead of empty interface
type ToolsProps = Record<never, never>;

const Tools: React.FC<ToolsProps> = () => {
  return <></>;
};

export default Tools;
