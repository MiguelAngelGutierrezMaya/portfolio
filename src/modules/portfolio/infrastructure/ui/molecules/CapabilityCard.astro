---
interface Props {
  index: string;
  title: string;
  description: string;
  keywords: readonly string[];
  capabilitiesLabel?: string;
}

const { index, title, description, keywords, capabilitiesLabel = 'capabilities' } = Astro.props;
---

<article class="capability-card reveal">
  <span class="capability-card__index">{index}</span>
  <div>
    <h3>{title}</h3>
    <p>{description}</p>
  </div>
  <ul aria-label={`${title} ${capabilitiesLabel}`}>
    {keywords.map(keyword => <li>{keyword}</li>)}
  </ul>
</article>
